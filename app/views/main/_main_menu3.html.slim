div.container
	div.row
		div
			h1
				' 미결제 목록 (결재대기중)
			p
				ul
					- @reports.each do |report|
						- if report.target
							li
								- if report.status == :reporting
									span.label.label-info= report.localize_status
								- else
									span.label.label-warning= report.localize_status
								= link_to report.target.summary, report.target
/!		div.span4
/!			= render partial: "this_week"
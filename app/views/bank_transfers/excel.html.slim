- form_type = if bank_transfer.new_record? then 'new' else 'edit' end

#show_command
  = link_to t('common.title.back'), [bank_account, :bank_transfers]

h2= t("common.title.#{form_type}")
h3 아직 준비되지 않았습니다
<div id="top-menu">
	<div id="account">
		<ul>
			<li>
				<%= link_to t("users.my.title")+(@user != nil ? "(#{@user.name})" : ""), users_my_path %>
			</li>
			<li>
				<%= link_to t("common.link.logout"), users_logout_path %>
			</li>
		</ul>
	</div>
	<!-- <div id="loggedas">
		<%= t "common.title.loggedas" %> : <%#= @user.name %>
	</div> -->
	
	<ul>
		<li>
			<%= link_to t("common.title.main"), :controller => "main", :action => "index" %>
		</li>
	</ul>
</div>

